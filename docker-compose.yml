services:
  shoplist:
    build: .
    image: shoplist:latest
    container_name: shoplist
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - shoplist-data:/app/data
    restart: unless-stopped

volumes:
  shoplist-data:
